name: Deploy Docs

# Docs build is not yet configured. Re-enable the push trigger when mkdocs (or equivalent) is set up.
on:
  workflow_dispatch:

jobs:
  call-docs:
    uses: django-mvp/workflows/workflows/docs.yml@main
    secrets: inherit
