name: Dependency Updates

on:
  schedule:
    # Run weekly on Mondays at 9 AM UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:

jobs:
  call-dependencies:
    uses: django-mvp/shared/.github/workflows/dependencies.yml@main
    secrets: inherit
